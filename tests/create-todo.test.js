const createTodo = require('../src/create-todo');

test('title to be title', () => {
  expect(createTodo.title).toBe() ;
});

// test('Create todo should have title feature', ()=> {
//   expect(typeof createTodo().toBe('function'));
// });